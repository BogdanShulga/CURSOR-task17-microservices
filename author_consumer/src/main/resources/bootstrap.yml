eureka:
  client:
    serviceUrl:
      defaultZone: http://discovery-server:8761/eureka
      fetch-registry: true
      registerWithEureka: true
spring:
  application:
    name: author-consumer
  cloud:
    config:
      discovery:
        enabled: true
        serviceId: config
      failFast: true